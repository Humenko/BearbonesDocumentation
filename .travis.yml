language: node_js

node_js:
  - 'stable'

cache:
  directories:
    - node_modules

script:
  - npm run build

deploy:
  provider: firebase
  skip_cleanup: true
  token:
    secure: $FIREBASE_TOKEN
  on:
    branch: master

notifications:
  email:
    on_failure: change
